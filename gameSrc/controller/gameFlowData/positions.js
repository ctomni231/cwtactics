/**
 * Position object ( instance of `cwt.Position` ) with 
 * rich information about the selected position by an 
 * action and some relations.
 */
cwt.gameFlowData.source = new cwt.Position();

/**
 * Position object ( instance of `cwt.Position` ) with 
 * rich information about the selected position by an 
 * action and some relations.
 */
cwt.gameFlowData.target = new cwt.Position();

/**
 * Position object ( instance of `cwt.Position` ) with 
 * rich information about the selected position by an 
 * action and some relations.
 */
cwt.gameFlowData.targetselection = new cwt.Position();
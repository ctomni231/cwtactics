/**
 * Game is a central class which provides the game logic as 
 * API.
 * 
 * @namespace
 */
cwt.Game = my.Class({
  initialize:function(){
    throw Error("cannot instantiate abstract static class");
  }
});
package org.wolftec.cwtactics.game.system.old;


public class ScreenSystem {
  //
  // @Override
  // public void onConstruction() {
  //
  // events().MOUSE_CLICK.subscribe((x, y, key) -> {
  // info("mouse click at " + x + " " + y);
  //
  // events().SET_CURSOR.publish(x, y);
  //
  // if (key == "LEFT") {
  // events().CURSOR_ACTION.publish();
  // } else if (key == "RIGHT") {
  //
  // }
  // });
  // }
}

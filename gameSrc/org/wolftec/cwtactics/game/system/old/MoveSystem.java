package org.wolftec.cwtactics.game.system.old;


public class MoveSystem {
  //
  // public void moveEntity(String id, Array<Integer> path) {
  // Position posC = entityManager().getComponent(id, Position.class);
  // MovingAbilityCmp moveableC = entityManager().getComponent(id,
  // MovingAbilityCmp.class);
  //
  // int cX = posC.x;
  // int cY = posC.y;
  // int oX = cX;
  // int oY = cY;
  // int cFuel = moveableC.fuel;
  //
  // // TODO
  //
  // posC.x = cX;
  // posC.y = cY;
  //
  // publishEvent("unit/moved");
  // }
}

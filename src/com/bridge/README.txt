This is the bridge section for code using ST-JS.

The hope is that the CWT group can use the code only within this folder to convert to JS. 
Java will then use the outside code to make a fully workable environment.
JavaScript will use the converted code to make its fully workable environment.
Hopefully, if this runs as planned, it'll be the win win situation proposed.
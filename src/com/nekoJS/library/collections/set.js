neko.define("collections/set",function(){

    /**
     * Set class.
     *
     * @class
     */
    var Set = neko.Class( /** @lends Set# */ {

        constructor : function( initSize ){
        },

        add : function( obj ){
        },

        insertAt : function( obj, index ){
        },

        set : function( obj, index ){
        }
    });

    return{

        VERSION : 1.0,

        Set : Set
    }
})
meow.sys.reqModule("MeowJSON")
meow.sys.reqModule("MeowStorage")

/**
 * MeowEngine's persitence module, for serializing data from meow
 * context to files and generating objects from files.
 *
 * @namespace
 */
meow.save =
/** @lends meow.save# */
{

	

	saveGame : function( name ){},

	loadGame : function( name ){}
}